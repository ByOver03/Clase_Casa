plugins {
    id 'java'
}

java {
    toolchain {
        // target Java 21 (LTS)
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // add dependencies here if needed
}
